Standard text input. Hairline border, small radius, vermillion focus ring + tint. Pass an `icon` for search fields.

```jsx
<Input label="Email" type="email" placeholder="you@domain.com" />
<Input placeholder="Search 4.2M records…" icon={<SearchIcon/>} />
```
