The house conversion unit — inline email input + vermillion Subscribe button. Appears in masthead, footer and the newsletter. Composes `Button`.

```jsx
<SubscribeField cta="Subscribe" note="One email a week · unsubscribe anytime" />
```
