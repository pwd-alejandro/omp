Inline source citation. Core to the brand's rigour — every data claim carries provenance. Used mid-sentence and in Assistant answers.

```jsx
…rose 40% since 2016 <SourceChip n={3} source="County records" href="#s3" />.
```

Keep the `source` label to 1–2 words. Fills vermillion on hover.
